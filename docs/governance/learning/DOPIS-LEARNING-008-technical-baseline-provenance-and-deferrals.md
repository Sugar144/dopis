# DOPIS-LEARNING-008 — Technical baseline provenance and deferral binding

- Observation: independent review found three PLAN-003 baseline decisions (API contract style, staff-authentication backend enforcement, PostgreSQL/Alembic as authoritative store) classified `NEW_MINIMUM_BASELINE` or `INHERITED_ACCEPTED` when discovery §3.2 already recorded them as provisional, and two deferrals (staff identity/credential/session implementation, SMS provider selection) bound to production-hosting/production-readiness timing later than their earliest real dependency.
- Impact: a mixed-provenance decision could misrepresent a promoted discovery direction as newly authored or independently accepted, and a deferred choice bound to too late a phase could block a vertical (protected-staff auth, SMS-dependent communications) on an unrelated later decision (production hosting) instead of its own earliest actual dependency.
- Cause: classification and deferral-timing were assigned per decision without checking each one against discovery §3.2's provisional list and against the accepted requirement/readiness milestone that actually determines when it must resolve.
- Containment: preserved the resulting REST/HTTP/OpenAPI, server-enforced-authorization, and PostgreSQL/Alembic baseline semantics; preserved `NOT_GRANTED` implementation authority; changed no requirement, story, use case, gate, exclusion, traceability artifact, validator, or code.
- Correction: reclassified `TB-API-001`, `TB-AUTH-001`, and `TB-DATA-001` to `PROMOTE_PROVISIONAL` where discovery §3.2 already recorded the direction as provisional, split out the genuinely new minimum decisions (`TB-API-004`, `TB-AUTH-004`) that discovery does not itself state, split `TB-AUTH-003` so only cookie domain/origin configuration stays bound to production-hosting topology while identity provisioning/credential-reset/session-storage implementation (`TB-AUTH-005`) resolves by the protected-staff vertical independent of hosting, and rebound `TB-INTEGRATION-003` SMS provider selection to the communications/messaging vertical's pilot-readiness provisioning ahead of the first real pilot order (FR-COMMS-001, FR-COMMS-002) instead of generic production-readiness timing.
- Prevention: before assigning a baseline decision's classification or a deferral's resolution timing, check it against canonical discovery's provisional list and against the accepted requirement/readiness milestone that actually determines the earliest point it must resolve, rather than the most convenient later phase.
- Evidence limits: this record corrects baseline provenance/classification and deferral-resolution timing only; it grants no implementation authority, decides no concrete API/auth/SMS implementation, and does not start vertical-slice planning.
- Validation plan: run the locked GOV-GEN consumer validator, `scripts/validate_specification.py`, `scripts/test_validate_specification.py`, and confirm baseline JSON decision-ID uniqueness, classification vocabulary, deferred-entry/decision matching, referenced-artifact resolution, and `git diff --check`.
