# DOPIS-LEARNING-007 — Story semantic boundaries and material constraints

- Observation: a populated capacity story combined Jaime’s normal-service authority with authorised staff and responsible-person temporary current-service actions; story acceptance criteria also covered observable constraint-defined outcomes without CONSTRAINT links.
- Impact: a story could appear traceable while blurring actor authority or allowing materially different product outcomes to pass under behavior-only coverage.
- Cause: decomposition and coverage checks concentrated on behavior requirements without testing whether an acceptance boundary was defined by a parent constraint.
- Containment: preserve the accepted use-case model, requirements, gates, exclusions, contracts, and all unresolved operational values.
- Correction: split the capacity story by actor goal and authority boundary, and link only parent constraints that determine an observable story acceptance outcome.
- Prevention: every product acceptance statement must trace the accepted requirement that determines it, including a parent CONSTRAINT when that constraint defines the observable acceptance boundary; a story must retain one coherent actor goal and authority boundary.
- Evidence limits: this record corrects backlog semantics and traceability only; it grants no implementation authority and does not calibrate or resolve any open value.
- Validation plan: run the locked GOV-GEN consumer validator, specification validator, negative-fixture suite, and formatting check.
