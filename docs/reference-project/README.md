# University reference project

The original university e-commerce project is retained as local reference evidence.

- Expected archive: `submission_PR_Brian.zip`
- Expected local path: `docs/reference-project/submission_PR_Brian.zip`
- Git policy: the ZIP is intentionally ignored because it is large and contains generated dependencies.
- Usage: read-only architectural reference.
- Code reuse: not authorised without a separate review.

Do not commit `node_modules`, bundled backend artifacts, secrets, or generated build output from this archive.
